<div {{ $attributes->class(['mx-auto max-w-2xl text-center']) }}>
    <h1 class="text-5xl mb-2 font-bold font-sans">Onset</h1>
    <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">{{ trans('auth.unleash_your_outdoor_adventure') }}</h2>
    <p class="mt-2 text-lg leading-10 text-gray-600">
        {{ trans('auth.your_gateway_to_thrilling_outdoor_experiences') }}
        {{ trans('auth.start_your_journey_today') }}
    </p>
</div>
